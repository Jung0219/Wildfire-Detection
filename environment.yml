name: wildfire-detection
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch
  - torchvision
  - pip:
      - ultralytics
      - opencv-python
      - numpy
      - pillow
      - pyyaml
      - tqdm
      - matplotlib
      - scikit-learn
      - scikit-image
      - pycocotools
      - timm
